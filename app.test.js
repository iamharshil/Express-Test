const request = require('supertest');
const app = require('./app.js')

describe("TODOs API", () => {
  it('GET /todos --> array todos', () => {})
  it('GET /todos --> specific todo by ID', () => {})
  it('POST /todos --> created todos', () => {})
  it('GET /todos --> validates request body', () => {})
})